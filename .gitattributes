# Auto detect text files and perform LF normalization
* text=auto
Datasets/combined_train.jsonl filter=lfs diff=lfs merge=lfs -text
Datasets/combined_validation.jsonl filter=lfs diff=lfs merge=lfs -text
